import React, { useState, useEffect, useRef } from "react";

export default function Etiqueta(props) {
  const { placeholder, olbigatorio, etiqueta } = props;

  return (
    <div>
      <input type="text" />
    </div>
  );
}
