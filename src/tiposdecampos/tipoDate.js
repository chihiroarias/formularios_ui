import Etiqueta from "../elementos/etiqueta/etiqueta";

export default function TipoDate(props) {
  const {} = props;

  return (
    <div>
      <Etiqueta />
      <input type="Date" />
    </div>
  );
}
